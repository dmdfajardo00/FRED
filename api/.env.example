DUCKDB_PATH=/data/fred.duckdb
PORT=3000
ORIGIN=https://db-dataviz.dmdfajardo.pro
NODE_ENV=production

# Legacy — no longer read; origin allowlist is hardcoded in hooks.server.ts.
# CORS_ORIGINS=

# Optional: server-to-server bypass. When set, X-API-Key: <value> allows
# requests from origins outside the browser allowlist (curl, Postman).
# Leave empty to disable the bypass entirely.
API_KEY=
